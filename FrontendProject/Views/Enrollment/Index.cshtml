@model IEnumerable<FrontendProject.Models.Enrollment>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">List Course</h6>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <p>
                <a class="btn btn-primary" asp-action="Create">Create New</a>
                <input type="submit" value="Pagging" class="btn btn-secondary" data-toggle="modal" data-target="#Modal1" />
            </p>
            <p>
                @if (ViewData["pesan"] != null)
                {
                    @Html.Raw(ViewData["pesan"])
                }
            </p>
            <table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.EnrollmentID)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Course.Title)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Student.FirstMidName) @Html.DisplayNameFor(model => model.Student.LastName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Grade)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.EnrollmentID)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Course.Title)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Student.FirstMidName)  @Html.DisplayFor(modelItem => item.Student.LastName) 
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Grade)
                            </td>
                            <td>
                                <a class="btn btn-success" asp-action="Update" asp-route-id="@item.EnrollmentID">Update</a> &nbsp;
                                <a class="btn btn-warning" asp-action="Details" asp-route-id="@item.EnrollmentID">Details</a> &nbsp;
                                <a class="btn btn-danger " asp-action="Delete" asp-route-id="@item.EnrollmentID">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
  
<!-- Modal Pencarian Skip Take-->
<div class="modal fade" id="Modal1" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
     aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Cari Berdasarkan Skip dan Take</h5>
                <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <div class="modal-body">

                <form asp-action="Index" method="get">
                    <input type="number" name="skip" class="form-control" placeholder="skip" />
                    <br />
                    <input type="number" name="take" class="form-control" placeholder="take" />
                    <br />
                    <input type="submit" value="Submit" class="btn btn-warning" />
                </form>
            </div>
        </div>
    </div>
</div>

